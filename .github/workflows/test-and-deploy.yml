name: Test & Deploy

on:
  push: 
    branches:
     - main

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      TZ: "Europe/London"

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 23
      
      - name: Use PostgreSQL
        uses: harmon758/postgresql-action@v1
        with:
          postgresql-version: 17
          postgresql db: ${{ secrets.PGDATABASE }}
          postgresql user: ${{ secrets.PGUSER }}
          postgresql password: ${{ secrets.PGPASSWORD }}
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run Tests - Articles
        run: PGDATABASE=${{ secrets.PGDATABASE }} PGUSER=${{ secrets.PGUSER }} PGPASSWORD=${{ secrets.PGPASSWORD }} npm test __tests__/articles-endpoints.test.js
        env:
          PGDATABASE: ${{ secrets.PGDATABASE }}
          PGUSER: ${{ secrets.PGUSER }}
          PGPASSWORD: ${{ secrets.PGPASSWORD }}
      

      - name: Run Tests
        run: PGDATABASE=${{ secrets.PGDATABASE }} PGUSER=${{ secrets.PGUSER }} PGPASSWORD=${{ secrets.PGPASSWORD }} npm test
        env:
          PGDATABASE: ${{ secrets.PGDATABASE }}
          PGUSER: ${{ secrets.PGUSER }}
          PGPASSWORD: ${{ secrets.PGPASSWORD }}
